# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-firebase
dist-ssr
.firebase
*.local
.env
.env.*
!.env.example
# Khóa bí mật Google Service Account
*-*.json
!package.json
!tsconfig*.json
!*.oxlintrc.json

# Trang duyệt hình cấu tạo (đồ dev, sinh bởi npm run struct)
structure-review.html

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Trang duyet de cho thay co - in ra soi roi bo, khong can luu
de-review-*.html

# Kho video truyền thông — 1,9 GB file media, KHÔNG đưa vào git.
# Video dựng lại được bằng các script gen-*.mjs nên không cần lưu trữ ở đây.
promo/**/*.mp4
promo/**/*.mp3
promo/**/*.wav
promo/**/*.png
promo/**/*.jpg
promo/**/*.jpeg
promo/**/*.txt
# nhưng giữ lại báo cáo và trang web trong promo/
!promo/*.md
!promo/*.html
